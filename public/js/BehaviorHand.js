export class BehaviorHand {
	constructor(args) {
		let props = args.description
		let blox = args.blox
	}
	// this would try be in front of the pov, and it would be physics based and kinematic, to do both?
	// can be used for many things
}

