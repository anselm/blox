
export class BehaviorPortal { //extends THREE.Reflector {
/*
	constructor(args) {
		let props = args.description

        let width = props.width || 1
        let height = props.height || 1
        let color = props.color || "white"
        let clear = props.clear || "red"
        var geometry = new THREE.PlaneBufferGeometry( width, height )
        let portal = super( geometry, {
          scene:scene,
          clipBias: 0.001,
          textureWidth: 512,
          textureHeight: 512,
          color: color,
          clear: clear,
          recursion: 1
        } )
		super()
    }

	on_blox_added(args) {
		let blox = args.blox
		let child = args.child
		let mesh = this
		let children = child.query({instance:THREE.Object3D,all:true})
		children.forEach((value)=>{
			console.log("*******  mesh named " + args.blox.name + " adding child named " + args.child.name)
			value.visible = false
			mesh.add(value)
		})
		return false // don't continue to pass this fact on
	}
*/

}
